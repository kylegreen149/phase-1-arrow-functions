const divide = function (num1, num2) {
    let quotient = num1 / num2
    return quotient
}

const square = numToMultiply => numToMultiply ** numToMultiply

const add = (num3, num4) => {
    let sum = num3 + num4
    return sum
}